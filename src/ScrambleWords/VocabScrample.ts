// <h1>Vocabulary Scramble</h1>
//       {randomizedList.map((word, index) => {
//         return (
//           <ScrambleBox
//             key={word.id}
//             vocabularyWord={helpers.scrambleWord(word.word, 10)}
//             id={index + 1}
//             hintPhrase={word.hint}
//           />
//         );
//       })}